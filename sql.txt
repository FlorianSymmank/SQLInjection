CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name text NOT NULL,
    address character,
    salary real, 
);

INSERT INTO users (name, address, salary) VALUES ('Hans', 'Wilhelminenhofstrasse', 45.4);